"use strict";

const {
  EXECUTION_SHAPE_DECIDER_VERSION,
  SHAPE,
  createExecutionShapeDecider,
} = require("./ExecutionShapeDecider");

module.exports = {
  EXECUTION_SHAPE_DECIDER_VERSION,
  SHAPE,
  createExecutionShapeDecider,
};

