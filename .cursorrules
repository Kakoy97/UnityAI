# Unity MCP Planner-Only 执行规则（Phase1 Step6）

## 0. 核心原则
- 所有 Unity runtime 读写请求，统一走 `planner_execute_mcp`。
- 禁止从客户端直接调用 runtime 工具（如 `set_*`、`add_component`、`execute_unity_transaction`、`get_scene_roots` 等）。
- Control/support-plane 工具：可直接调用，用于查询约束/契约，不用于替代 Planner runtime 执行。

## 1. 推荐执行流程（默认）
1. 先判断是否需要 schema/contract 信息。
2. 需要时先调 `get_write_contract_bundle`，再按需调 `get_tool_schema`。
3. 组装 `planner_execute_mcp` 的 `block_spec` 并提交。
4. 依据 `BlockResult` 决定后续步骤。

## 2. block_spec 约束
- 优先使用 family 语义键（`family_key` / family intent），不要新增 legacy concrete key 依赖。
- `block_spec` 必须包含：`block_id`、`block_type`、`intent_key`（或可映射 family key）、`input`。
- 写入请求必须保证 `write_envelope` 与锚点信息完整、可追溯。
- 提供 `target_anchor` 时，`write_envelope` 字段自动补全；`based_on_read_token` 需手动传递。

## 3. 事务使用规则
- **多个写操作必须使用事务**：当需要执行 2 个或以上写操作（`MUTATE`/`CREATE`）时，必须使用 `write.transaction.execute` intent_key 组合为单个事务。
- 事务格式：使用 `intent_key: "write.transaction.execute"`，在 `input.steps[]` 中定义多个步骤。
- 禁止将多个写操作拆分为独立的 `block_spec` 分别执行（除非操作间无依赖且允许部分失败）。

## 4. 错误处理（最多一轮修复）
- `E_SCHEMA_INVALID`：
  - **优先检查错误响应的 `details` 字段**：如果包含 `supported_family_keys`、`supported_legacy_concrete_keys` 等直接可用的修复信息，优先使用这些信息修复 payload。
  - 如果错误响应中没有直接可用的修复信息，再查 schema/contract（`get_write_contract_bundle` 或 `get_tool_schema`），然后修 payload。
  - 禁止跳过错误响应直接去代码库搜索。
- `E_PRECONDITION_FAILED`：先补前置读或锚点，再重试一次。
- `E_USE_PLANNER_ENTRY`：只允许改走 `planner_execute_mcp`，禁止 direct fallback。
- 同一错误禁止盲目多轮重试；二次失败后返回阻塞点与最小修复建议。

## 5. Script Workflow Hard Rules (minimal)
**⚠️ 场景识别：创建脚本 + 附加组件 → 必须使用此规则**
- For "create script + wait compile + attach component", always use `planner_execute_mcp` with:
  - `block_type: MUTATE`
  - `intent_key: workflow.script.create_compile_attach`
- `input.thread_id` is required.
- Do not ask user to wait for C# compile manually; workflow must handle waiting internally.
- Keep `submit_unity_task` oneOf semantics:
  - file step uses `file_actions` only
  - attach step uses `visual_layer_actions` only
- Success must include `planner_orchestration.workflow_template_applied === true`.
- If `workflow_template_applied !== true`, treat as incomplete and continue fixing (do not end task as success).
