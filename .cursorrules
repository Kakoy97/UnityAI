# Unity MCP SSOT 执行规则（低 Token 版）

## 目标
- 首次执行成功率优先。
- 同一任务最少工具调用次数优先。
- 禁止猜测事务引用格式、属性路径、组件类型。

## 一、查询策略（先少后准）
1. 默认先用 function definitions。
2. 命中以下任一条件时，再查询 SSOT：
   - 使用 `execute_unity_transaction`。
   - 首次使用某个复杂写工具（如 `set_component_properties`、`set_serialized_property`）。
   - 出现错误码后需要定向修复。
3. 优先查询顺序：
   - `get_write_contract_bundle`（拿最小有效模板）
   - `get_tool_schema`（看字段约束）
4. 禁止直接全量扫描大文件（如整份 `mcp-tools.generated.json`），除非排障明确需要。

## 二、事务规则（强约束）
1. `execute_unity_transaction` 使用结构化 `steps`，不用 `steps_json`。
2. `$ref` 只能使用对象形式：
   - 正确：`{"$ref":"alias.field"}`
   - 错误：字符串插值、混合对象（`$ref` + 其他字段）。
3. 引用来源是 `save_as` 别名，不是 `step_id`。
4. 当前允许的别名字段：
   - `target_object_id`
   - `target_path`
   - `target_object_name`
   - `scene_revision`
   - `value_kind`
   - `value_string`
   - `value_number`
   - `value_boolean`
5. 常见错误修正：
   - `container.object_id` -> `container.target_object_id`
   - `container.path` -> `container.target_path`

## 三、属性写入规则
1. `set_component_properties`/`set_serialized_property` 的 `property_path` 使用 Unity 序列化路径（如 `m_Spacing`、`m_Color.a`），不要用 C# 属性名（如 `spacing`）。
2. `component_type` 优先使用：
   - `"Namespace.Type, AssemblyName"`（例如 `"UnityEngine.UI.HorizontalLayoutGroup, UnityEngine.UI"`）
3. 写属性前若不确定路径，先用 `get_serialized_property_tree` 查一次后复用，不重复查。

## 四、Token 与调用次数控制
1. 写操作会改变 `scene_revision`，旧 token 不可继续复用。
2. 优先把相关写操作收敛到一次事务内，减少 token 往返。
3. 同一任务默认流程：
   - 读锚点/令牌 1 次
   - 事务写 1 次
   - `save_scene` 1 次
   - 验证截图最多 1 次（仅用户要求验收时）
4. 非必要禁止：
   - 重复 `get_scene_snapshot_for_write`
   - 重复 schema 查询
   - 为“确认而确认”的多次读操作

## 五、批量 UI 任务执行模板（推荐）
适用任务：批量创建 UI、统一布局、设置颜色/文本、并保存场景。

1. `get_scene_snapshot_for_write`（一次）
2. `execute_unity_transaction`（一次，包含）
   - 创建容器（可 `save_as`）
   - 添加/设置 Layout 组件（例如 `HorizontalLayoutGroup` + `m_Spacing`）
   - 创建 N 个子节点
   - 通过 `$ref` 绑定每个子节点并设置颜色/位置/尺寸
3. `save_scene`（一次）
4. `capture_scene_screenshot`（可选，一次）

## 六、失败处理（最多一轮修复重试）
1. 先读错误码，再定向查询一次：
   - `E_TRANSACTION_REF_PATH_INVALID` -> 查事务别名字段与 `$ref` 结构
   - `E_PROPERTY_NOT_FOUND`/`E_PROPERTY_TYPE_MISMATCH` -> 查 `property_path` 与 `value_kind`
   - `E_COMPONENT_NOT_FOUND` -> 查 `component_type` 格式
2. 修复后只重试一次；仍失败则返回明确阻塞点，不进行盲试。

## 七、移除的旧做法（禁止）
- 禁止规则里引用“固定文件行号”（产物更新后会漂移失效）。
- 禁止“写后直接复用返回 token”与“写后 token 无效”并存的矛盾规则。
- 禁止把“并行调用”用于有依赖的写步骤；有依赖写步骤必须用事务或串行。

## SSOT 关键位置
- `ssot/dictionary/tools.json`
- `ssot/artifacts/l2/mcp-tools.generated.json`
- `ssot/artifacts/l3/SsotDtos.generated.cs`
