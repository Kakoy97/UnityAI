# Unity MCP SSOT 工具使用规则

## SSOT字典查询策略

### 原则
1. **常用工具**：直接使用工具定义（function definitions），它们已包含基本参数信息
2. **复杂/不确定时**：查询SSOT字典获取L2/L3规则和详细指导
3. **遇到错误时**：查询SSOT字典，查看 `include_error_fix_map` 获取修复方案

### 查询时机
- ✅ **不需要查询**：简单操作、参数明确、工具定义已足够
- ✅ **需要查询**：
  - 参数不确定或需要了解L2/L3门禁规则
  - 遇到验证错误需要查看修复指导
  - 复杂操作需要查看examples

### 查询方式
- 使用 `get_tool_schema` 获取工具架构信息
- 使用 `get_write_contract_bundle` 获取写入合约（包含examples和error_fix_map）

### 禁止行为
- ❌ 不要一次性查询所有工具的SSOT字典（token消耗大，易产生幻觉）
- ❌ 不要每次都查询（效率低）
- ❌ 不要忽略工具定义中的基本参数信息

## SSOT字典位置
- 源文件：`ssot/dictionary/tools.json`
- L2产物：`ssot/artifacts/l2/mcp-tools.generated.json`
- L3产物：`ssot/artifacts/l3/SsotDtos.generated.cs`

## 性能优化策略

### 减少调用次数
1. **避免不必要的查询**：
   - 不需要分别查询x和y坐标，可以直接设置位置
   - 创建对象后可以直接使用返回的token，不需要重新获取快照
   - 尽量一次性获取所需信息

2. **复用token**：
   - `create_object`等写操作返回后，可以直接使用返回的`read_token_candidate`
   - 不需要每次都调用`get_scene_snapshot_for_write`

3. **批量操作**：
   - 如果可能，使用`execute_unity_transaction`一次性完成多个操作
   - 减少往返通信次数

4. **并行调用**：
   - **无依赖关系的操作可以并行**：同时发起多个工具调用（如同时读取多个不相关的信息）
   - **有依赖关系的操作必须串行**：后一个操作依赖前一个操作的结果
   - **有依赖关系的操作优先使用事务**：使用`execute_unity_transaction`原子性完成多个步骤
   - 示例：
     - ✅ 可以并行：同时读取多个对象的组件信息
     - ❌ 必须串行：创建对象 → 设置位置（必须先创建才能设置）
     - ✅ 使用事务：创建对象 + 设置位置 + 设置文本（一次性完成）

### Token管理策略
1. **Token失效原因**：
   - Token TTL是3分钟，时间足够
   - 主要失效原因是`scene_revision`不匹配（写操作会改变场景版本）
   - 每次写操作后，旧的token立即失效

2. **最佳实践**：
   - ✅ **优先使用事务工具**：一次性完成多个操作，减少token失效机会
   - ✅ **写操作后立即获取新token**：如果必须分步操作，写操作后立即调用读工具获取新token
   - ❌ **不要复用写操作前的token**：写操作会改变scene_revision，旧token会失效
   - ❌ **不要依赖写操作返回的token**：写操作不会自动返回新token（read_token_candidate为空）

3. **操作流程优化**：
   - 简单操作：获取token → 执行操作
   - 复杂操作：获取token → 使用事务工具一次性完成
   - 分步操作：获取token → 操作1 → 获取新token → 操作2 → 获取新token → 操作3

### 延迟原因
- 每次调用都需要：MCP Client → Sidecar (HTTP) → Unity Editor (HTTP) → 返回
- 默认超时60秒，但每次往返都有延迟
- Unity Editor响应速度取决于当前负载
- Token失效导致需要重新获取token，增加往返次数