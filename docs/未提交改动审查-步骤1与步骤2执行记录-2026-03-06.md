# 未提交改动审查（步骤1+步骤2）执行记录

日期：2026-03-06  
分支：`main`  
工作区未提交条目数：`283`（`git status --short` 统计）

## Step 1：调用图与入口图审计结果

### 1.1 Router/Contract 入口图
来源文件：
1. `sidecar/src/ports/contracts.js`
2. `sidecar/src/api/router.js`

结论：
1. 活跃 MCP 写路由：`7`
2. 活跃 MCP 读路由：`22`
3. 活跃 MCP 状态路由：`3`（仅 `get_unity_task_status`、`cancel_unity_task`、`capabilities`）
4. Unity callback 路由：`7`
5. 已废弃路由：`11`（含 `/mcp/heartbeat`、`/mcp/metrics`、`/mcp/stream`）

补充：
1. Router 已统一对 `deprecated_http_routes` 返回 `410 E_GONE`。
2. `/mcp/heartbeat`、`/mcp/metrics`、`/mcp/stream` 不再是 Router 活跃分支。

### 1.2 definitions -> turnServiceMethod 映射
来源目录：`sidecar/src/mcp/commands/definitions`

结论：
1. `turnServiceMethod` 唯一方法数：`59`
2. 在 `turnService` 中缺失方法数：`0`
3. 结论：definitions 到 turnService 的映射完整，无断线。

### 1.3 turnService 方法全集与“非 definitions 方法”统计
来源文件：`sidecar/src/application/turnService.js`

结论：
1. `turnService` 方法总数：`102`
2. definitions 未引用的方法数：`43`
3. 这 `43` 个方法是 Step 2 判定对象。

## Step 2：KEEP / MIGRATE / REMOVE 判定

判定口径：
1. `KEEP`：Router 活跃调用、或 SSOT/底座内部调用链依赖。
2. `MIGRATE`：仅测试或兼容调用依赖，需先迁移调用口径再删除。
3. `REMOVE`：无 Router、无 definitions、无内部链路依赖、无测试依赖。

### 2.1 KEEP（本轮不删）
1. Router 直连能力：`getHealthPayload/getStateSnapshotPayload/getCapabilitiesForMcp/reportCompileResult/reportUnityActionResult/reportUnityRuntimePing/reportUnitySelectionSnapshot/reportUnityCapabilities/pullUnityQuery/reportUnityQuery`
2. SSOT 核心链路：`dispatchSsotToolForMcp/validateSsotTokenForMcp/enqueueAndWaitForUnityQuery`
3. 门禁与观测支撑：`validationError/recordMcpToolInvocation`
4. 内部辅助链路：`cleanupSessionCache/cleanupFileActionCache/resolveTurnApprovalMode/buildUnityActionRequestEnvelopeWithIds/resolveRequestIdFromFailureBody/mapFileErrorToStatus`

### 2.2 MIGRATE（需先迁移调用口径）
1. `getCurrentSelectionForMcp`
   - 现状：仅测试 helper 在调用（6 处）。
   - 迁移方向：改为 `getCurrentSelectionSsotForMcp`（异步）或统一走 command registry/mcp tool 调用口径。
2. `startSession`
   - 现状：仅测试 stub 中出现（1 处）。
   - 迁移方向：测试直接断言 Router 对 `/session/start` 的 `410`，不再依赖 turnService stub 方法。
3. `sendTurn`
   - 现状：仅测试 stub 中出现（1 处）。
   - 迁移方向：测试直接断言 Router 对 `/turn/send` 的 `410`，不再依赖 turnService stub 方法。

### 2.3 REMOVE（可直接进入删除清单）
1. `applyFileActions`
2. `getTurnStatus`
3. `cancelTurn`
4. `getCompileStateForMcp`
5. `getConsoleErrorsForMcp`
6. `listMcpResources`
7. `readMcpResource`
8. `reportUnityConsoleSnapshot`
9. `reportUnityQueryComponentsResult`
10. `getGameObjectComponentsForMcp`
11. `getHierarchySubtreeForMcp`
12. `getPrefabInfoForMcp`
13. `buildUnityActionRequestEnvelope`
14. `submitUnityQueryAndWait`
15. `captureLatestCompileSnapshot`
16. `captureLatestActionErrorSnapshot`
17. `normalizeWriteOutcome`（同时评估 `resolveRequestIdFromFailureBody` 连带收敛）

## 需要先通知的事项（按纪律）
本次 Step 2 已识别到一个“旧 -> 新”迁移触发点：
1. `getCurrentSelectionForMcp` 的测试依赖迁移到 `getCurrentSelectionSsotForMcp`/SSOT 调用口径会引入异步行为变更。
2. 该变更属于“旧链路测试辅助逻辑向新链路口径迁移”，执行前应先确认。

## 当前结论
1. Step 1 已完成：入口图、definitions 映射、turnService 方法全集与孤儿方法统计已完成。
2. Step 2 已完成：`KEEP/MIGRATE/REMOVE` 判定清单已形成，可进入 Step 3 实施。
