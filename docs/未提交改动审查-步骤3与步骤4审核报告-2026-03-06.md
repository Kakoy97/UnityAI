# 未提交改动审查：步骤3与步骤4审核报告

日期：2026-03-06

## 0. 审核范围与排除项
本报告针对当前未提交变更中的 `sidecar/src` 进行步骤3（实施层）与步骤4（门禁层）审核。  
排除项：`sidecar/src/mcp/commands/**`（指令与 action 移植内容不计入本报告）。

## 1. Step3 审核结论（你关心的核心）

结论：**不是全部都属于旧链路清理，存在基础设施层改动。**

### 1.1 排除指令/action 后的主要改动文件（16 项）
1. `sidecar/src/api/router.js`（M）
2. `sidecar/src/application/mcpGateway/mcpEyesReadService.js`（D）
3. `sidecar/src/application/mcpGateway/mcpEyesService.js`（D）
4. `sidecar/src/application/mcpGateway/mcpEyesWriteService.js`（D）
5. `sidecar/src/application/preconditionService.js`（D）
6. `sidecar/src/application/ssotRuntime/`（??）
7. `sidecar/src/application/turnPayloadBuilders.js`（M）
8. `sidecar/src/application/turnPolicies.js`（M）
9. `sidecar/src/application/turnPolicySchemaCompensation.js`（D）
10. `sidecar/src/application/turnService.js`（M）
11. `sidecar/src/application/unityDispatcher/runtimeUtils.js`（M）
12. `sidecar/src/domain/validators/_mcpWriteValidatorsImpl.js`（D）
13. `sidecar/src/domain/validators/mcpWriteValidators.js`（M）
14. `sidecar/src/mcp/commandRegistry.js`（M）
15. `sidecar/src/mcp/mcpServer.js`（M）
16. `sidecar/src/ports/contracts.js`（M）

### 1.2 可判定为“旧链路清理”的改动
1. Router 侧下线旧路径：`/mcp/heartbeat`、`/mcp/metrics`、`/mcp/stream`（统一走 deprecated 410）。
2. `mcpEyes*`、`preconditionService`、`turnPolicySchemaCompensation` 删除。
3. `_mcpWriteValidatorsImpl.js` 删除，legacy write validator 统一改为 `410 E_GONE`。
4. 去除 `action_data_json` / `action_data_marshaled` 双线缆桥接字段（`turnPayloadBuilders/runtimeUtils`）。

### 1.3 已触达“基础设施”的改动（非纯旧链路）
1. `turnService.js`：核心服务装配与调用路径发生较大重构（含 SSOT runtime 接线、兼容层收缩）。
2. `mcpServer.js`：tools/list 能力快照与描述预算逻辑被移除，行为口径改变。
3. `commandRegistry.js`：schema compact 裁剪链路被移除，tools/list schema 输出策略改变。
4. `turnPolicies.js`：schema compensation 机制被硬关闭（返回 `null`）。
5. `ports/contracts.js`：路由冻结表、可见性策略、合同导出项发生改动。
6. `application/ssotRuntime/` 新增（当前为未跟踪目录），属于基础运行时能力层。

## 2. Step4 审核结果（门禁）

### 2.1 固定门禁（通过）
1. `protocol-write-consistency.test.js`：通过（4/4）
2. `ssot-batch-route.test.js`：通过（58/58）
3. `ssot-validator-registry.test.js`：通过（3/3）
4. `ssot-token-registry.test.js`：通过（4/4）
5. `ssot-revision-state.test.js`：通过（2/2）

### 2.2 兼容相关测试（失败，需口径迁移）
1. `job-lease-janitor.test.js`：7/7 失败  
2. `mcp-write-readiness-and-error-taxonomy.test.js`：4/5 失败  

共同失败特征：
1. 失败点集中在 `issueReadToken` 断言：`undefined !== 200`。
2. 说明测试 helper 仍依赖旧同步调用口径，未完全迁移到当前 SSOT 读 token 获取方式。

## 3. 审核判断（给你的直接回答）
1. “这些改动是否都是旧链路功能？”  
不是。当前改动中既有旧链路清理，也有基础设施层变更。

2. “有没有涉及基础设施？”  
有，且范围不小，主要在 `turnService/mcpServer/commandRegistry/turnPolicies/contracts/ssotRuntime`。

3. “主要改到了哪些（不含指令/action移植）？”  
已在 1.1 完整列出 16 项，旧链路与基础设施已在 1.2/1.3 分栏。

## 4. 建议（审查视角）
1. 将“纯旧链路清理”与“基础设施重构”拆分为两组提交或两批审核清单，避免风险混叠。
2. 先锁定基础设施文件白名单，未获确认不再继续改动上述 1.3 文件。
3. 兼容测试先做 helper 口径迁移，再判断是否存在真实基础能力回归。
